package h8io.cfg

//import cats.data.Validated
import org.scalamock.scalatest.MockFactory
import org.scalatest.flatspec.AnyFlatSpec
import org.scalatest.matchers.should.Matchers
import org.scalatestplus.scalacheck.ScalaCheckPropertyChecks

//import scala.collection.mutable

class ValuesOpsTest extends AnyFlatSpec with Matchers with MockFactory with ScalaCheckPropertyChecks {
//  "collectInto" should "collect values into a builder if all of them are valid" in
//    forAll { (values: Array[String]) =>
//      val builder = mock[mutable.Builder[String, Unit]]
//      values.iterator.map(Validated.valid).collectInto[Unit, mutable.Builder[String, Unit]](builder)
//    }
//
//  it should "collect all error to AndError" in {}
}
